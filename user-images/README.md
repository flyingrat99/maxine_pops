# Uploaded Pop photos

Photos uploaded in Maxine's Pop Tracker are resized and saved in this folder.
The image files are deliberately ignored by Git so personal collection photos
are not published with source-code updates. Use **Data & backup → Export full
backup** to make a portable backup that includes every uploaded photo.
