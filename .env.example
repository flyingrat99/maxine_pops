# Optional local server settings. The tracker works without this file.
PORT=4173
NO_OPEN=0

# Future marketplace connectors must remain server-side. Never prefix secrets
# with VITE_, because Vite exposes those values to browser code.
# EBAY_CLIENT_ID=
# EBAY_CLIENT_SECRET=
